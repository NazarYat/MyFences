<UserControl x:Class="MyFences.Controls.NumericUpToDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyFences.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <TextBox x:Name="PART_TextBox"
                 Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Style="{Binding TextBoxStyle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 PreviewTextInput="TextBox_PreviewTextInput"
                 PreviewKeyDown="TextBox_PreviewKeyDown"
                 TextChanged="PART_TextBox_TextChanged"
                 GotKeyboardFocus="PART_TextBox_GotKeyboardFocus"/>

        <!--<StackPanel Grid.Column="1" Orientation="Vertical">
            <Button Content="▲" Click="Up_Click" Padding="0" Height="14"/>
            <Button Content="▼" Click="Down_Click" Padding="0" Height="14"/>
        </StackPanel>-->
    </Grid>
</UserControl>
