<Window x:Class="MyFences.Windows.FenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fence"
        Height="100" Width="100"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True"
        Loaded="FenceWindow_Loaded">
    <Grid>
        
        <Grid Background="#33E26161" Opacity="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#33E26161" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <TextBlock Text="My App"
                           Foreground="White"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
                <Button Content="X" HorizontalAlignment="Right"
                        Width="20"
                        Click="Close_Click"
                        Background="Transparent" BorderBrush="{x:Null}" Foreground="White"/>
            </Grid>

            <Grid Grid.Row="1">
                <!--scrollable list here-->
            </Grid>
        </Grid>
    </Grid>
</Window>